@model IEnumerable<jScore3.Models.Pozycja>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Klub.Nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mecz.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Zawodnik.Imie)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nazwa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Klub.Nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mecz.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Zawodnik.Imie)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nazwa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
